// Production: the Angular app and the Spring Boot API are deployed separately.
// Set this to wherever your backend is hosted.
export const environment = {
  production: true,
  apiBase: 'http://localhost:8080/api'
};
